// Weather signal ingester — fetches local weather forecasts
// Placeholder: will connect to OpenWeather API in Phase 2+

export async function fetchWeather(templeId: string, city: string): Promise<void> {
  // TODO: Implement OpenWeather API integration
  console.log(`[weather] Fetching weather for temple=${templeId} city=${city}`);
}
